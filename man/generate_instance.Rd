% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/instances.R
\name{generate_instance}
\alias{generate_instance}
\title{Generate a Complete-Graph Instance for MO-MST}
\usage{
generate_instance(
  n,
  num_obj,
  range_a = c(10, 100),
  range_b = c(10, 50),
  range_c = c(30, 200),
  seed = NULL
)
}
\arguments{
\item{n}{Integer. Number of nodes of the graph (must be at least 3).}

\item{num_obj}{Integer in \{2, 3\}. Number of objectives.}

\item{range_a}{Numeric vector \code{c(min, max)} for weights of objective 1.}

\item{range_b}{Numeric vector \code{c(min, max)} for weights of objective 2.}

\item{range_c}{Numeric vector \code{c(min, max)} for weights of objective 3.
Ignored when \code{num_obj == 2}.}

\item{seed}{Optional integer. If supplied, the RNG seed is fixed before
sampling and restored afterwards, leaving the global RNG untouched.}
}
\value{
A \code{data.frame} with \code{n*(n-1)/2} rows and columns
  \code{from}, \code{to}, \code{weight_1}, \code{weight_2} (and
  \code{weight_3} when \code{num_obj == 3}).
}
\description{
Produces a complete undirected weighted graph with random multi-objective
edge weights. Each edge gets two or three independent uniform weights, one
per objective.
}
\examples{
inst <- generate_instance(n = 10, num_obj = 2, seed = 12345)
head(inst)
}
